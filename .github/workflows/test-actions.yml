varname: Test Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    environment: dev
    steps:
      - name: print greeting
        run: | 
          echo "Hello world!"
          echo "${{ secrets.NAME }}"
          echo "${{ vars.NAME }}"
      - name: print goodbye
        run: echo "bye!"
