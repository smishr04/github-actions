name: Test Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: print greeting
        run: | 
          echo "Hello world!"
          echo "hello world 2!"
      - name: print goodbye
        run: echo "bye!"
